// Program to demonstrate if comparison to be lodged between curly brackets in C with CodeBlocks
// chap - 08 page no - 102

// preprocessor directive
#include <stdio.h>

// Application starts with the main() function
int main()
{
    // Variable declaration and initialization
    int a, b;

    a = 5;
    b = -3;

    if(a == b);
    printf("%d equals %d\n", a, b);

    return (0);
}
