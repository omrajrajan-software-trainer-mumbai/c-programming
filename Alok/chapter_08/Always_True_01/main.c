// Program to demonstrate the value of a will be always true in C with CodeBlocks
// chap - 08  page no - 102

// preprocessor directive
#include <stdio.h>

// Application starts with the main() function
int main()

{
    // Variable declaration and initialization
    int a;

    a = -3;

    if (a = -3)
    {
        printf("%d equals %d\n", a, -3);
    }

    return (0);
}
