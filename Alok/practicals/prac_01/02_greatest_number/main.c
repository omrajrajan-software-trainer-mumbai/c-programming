// Program to find the greatest of three numbers using conditional operator in C with CodeBlocks

// Preprocessor directives
#include <stdio.h>

int main()
{
    printf("Program to find the greatest of three numbers using conditional operator in C with CodeBlocks\n\n");

    // Initialize three numbers let say a, b, c

    // Check if a is greater than b
    // If true then check if a is greater than c
        // If true then print 'a' as largest number
        // If false then print 'c' as largest number

    // If false then check if b is greater than c
        // If true then print 'b' as largest number
        // If false then print 'c' as largest number

    return 0;
}
