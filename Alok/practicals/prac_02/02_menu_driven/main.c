// Program to demonstrate a menu driven program using switch case to perform add or subtract or multiply or divide based on the users choice in C with CodeBlocks

// Preprocessor directives
#include <stdio.h>

// Application starts with the main() function
int main()
{
    printf("Program to demonstrate a menu driven program using switch case to perform add or subtract or multiply or divide based on the users choice in C with CodeBlocks\n\n");

    // create a character array as operation
    // Declare a variable a and b then initialize
    // Ask the user to Enter an operation
    // Ask the user to Enter two numbers
    // Start using switch
    // Create case of addition, subtraction, multiplication, division,
        //It prints the answer based on operation the user has taken or choosed
    // Create a default case that prints Enter a valid operation

    return 0;
}
