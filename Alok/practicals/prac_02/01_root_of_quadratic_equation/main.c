// Program to calculate roots of a quadratic equation in C with CodeBlocks

// Preprocessor directives
#include <stdio.h>

// Application starts with the main() function
int main()
{
    printf("Program to calculate roots of a quadratic equation in C with CodeBlocks\n\n");

    // Declaring float variable a, b, c, r1, r2, d
    // Ask to user Enter the values a, b, c
    // Compute d = b2 4ac
    // check if d is greater than 0 then
        // r1 = -b+sqrt (d) / (2*a)
        // r2 = -b-sqrt (d) / (2*a)
        // If it is true then print The real roots to %f %f", r1, r2
    // else if d is equal to the 0 then
        // r1 = -b/(2*a)
        //  r2 = -b/(2*a)
        // If it is true then print Roots are equal to %f %f", r1, r2
    // else print root is imaginary

    return 0;
}
