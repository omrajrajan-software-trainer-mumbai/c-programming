// program for goodbye source code
// page 13


#include<stdio.h>
#include<stdlib.h>

int main()
{
    // Variable Declaration and initialization
    int roll_no; // Declaration consist of data type and name of variable.
    roll_no = 21; // Initialization consists of assigning value to variable.

    int age = 25; // Declaration and initialization can also be done in one single statement.

    printf("Goodbye,World\nWelcome to the World of C programming.");
    printf("\nThe roll number is %d", roll_no); // %d is called the format specifier for integer data types.
    printf("\nThe Age is %d", age); // %d is called the format specifier for integer data types.

    return 0;
}
